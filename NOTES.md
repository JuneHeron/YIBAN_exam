# 观察记录

## 2026-09-22 平台题库存在重复条目

**观察**：第二次模拟考里又出现了「我校的校训是()」这道题。

**推论**：如果同一道题在**一场考试之内**出现两次，那说明平台自己的题库里就有重复条目
（一场考试是从题库里抽题，抽到两次同一道，只能是题库里存了两份）。

**影响**：平台的题量是名义上的（例如 250 道）。重复条目占掉了名额，所以按去重规则收集时
**永远凑不齐 250 道不同的题**——收集到的「新题」里有一部分是重复。判断进度时要用
「实际不同题数」，而不是收集次数。

### 怎么确认

两种信号，可靠性完全不同：

| 信号 | 可靠性 | 怎么得到 |
| --- | --- | --- |
| **同一场考试内出现重复题** | 硬证据，不需要判断 | 导入时自动报出 |
| 题干高度相似但没合并 | 只是候选，必须人工判断 | `python bank.py dupes` |

导入时的输出会明确分开这两种重复：

```
!! 同一场考试内重复 1 道 —— 平台题库自己就有重复条目:
   - 我校的校训是()。
跨场重复 2 道（此前已录过，正常）:
   - 学校设有()个教学学院，()个校区
```

**所以每次考完导入时，注意看有没有「同一场考试内重复」那一段。**
每出现一次，就是平台题库有重复的一个实证。攒起来就能估出题库的真实题量。

### dupes 的陷阱（重要）

`dupes` 靠文本相似度找候选，而**文本相似度无法判定是否同一道题**。已实测到的两种骗人情形：

- **正反问法**：「学生证包含哪些基本信息」与「学生证包含的基本信息不包括」——题干相似度
  0.83，但确实是两道题（答案内容完全不同）。
- **选项换写法**：「退役后()年」的选项是 `1/2/3/4`，「至退役后几年」的选项是
  `一年/三年/两年/四年`——选项文本零重合，但其实是同一道题（答案都是 2 年）。

所以 `dupes` 只输出事实（题干相似度、选项文本重合度、两侧答案的选项原文），
**结论由人下**。看 `→` 那行答案的选项原文最有用：意思一样就是重复，不一样就是两道题。

### 目前已确认的重复对

| 组 | 判定 | 依据 |
| --- | --- | --- |
| #56 / #61 | 同一道题 | 选项文本 100% 相同，答案原文都是「入学或复学当年」 |
| #17 / #81 | 同一道题 | 答案原文「2」与「两年」含义相同 |
| #68 / #79 | 两道不同的题 | 一正一反，答案原文完全不同 |
| #22 / #45 | 两道不同的题 | 答案原文不同（重复作弊者 / 侵害他人权益） |

### 另一类重复：平台的题干改写

平台会把同一道题换个问法，例如「应在()申请学费资助」与「在什么时间申请学费资助」。
这类**不会被去重合并**（题干正是判定的依据，放宽会把不同的题误判成一道），
所以会作为两道题分别收录。它们不是「平台的重复条目」，只是问法变体，
好处是两种问法都覆盖到了。

## 2026-09-23 通用题干撑不起题目身份（已修）

**观察**：`下列说法错误的是：()` 这道题，平台实际有两道不同的题共用这一句题干
（一道考「已享受服兵役资助的毕业生能否同时享受基层就业学费补偿」，另一道考其他条款），
正确答案分别是 A 和 D。

**为什么危险**：题目身份只取题干时，这两道会被合并成一行，答案互相覆盖——
查询时给出的字母可能是另一道题的答案。**错答案比没答案更糟**，这是必须修的。

**修法**：题干是纯套话时（`is_generic_stem()`：短于 8 字，或剥掉套话后剩不到 6 字），
把**选项集合**（排序后取，打乱顺序仍算同一道）也算进题目身份。于是两个变体各存一行、
各给各的答案；而同一道题选项被打乱时仍能合并。

判定刻意做窄，只覆盖真撑不起身份的题：`关于勤工助学，下列说法错误的是` 因为有主题前缀
（`关于勤工助学`）不算套话，保持正常合并——否则平台一改选项措辞就会多出一条。

## 2026-09-23 冲突比较要用选项原文，不能用字母（已修）

**踩到的坑**：同一道通用题干的题，选项打乱后重导，答案字母从 D 变成 A（同一个答案、不同顺序），
冲突检测比的是字母，于是**误报冲突**。

**修法**：冲突判定改为「字母相同 **或** 答案的选项原文相同」都算同一答案。字母不同但原文一致时
按正常更新处理，且字母和选项一起刷新，保证两者始终对得上同一次顺序。

**同时验证了真冲突仍会报警**：故意把答案指到另一个选项时，冲突表正常记录，且库里原答案不被覆盖。

## 2026-09-23 油猴脚本采集的题干被污染（已修）

**现象**：脚本采集入库的 20 条题干全部长这样：

```
查看试卷 冲！随机选择 一、单选题 11、[1分] 休学警告决定由()做出，试读决定由学院审核后报教务处审批发文。
```

**根因**：「查看试卷 / 冲！ / 随机选择」**是另一个油猴脚本注入的按钮文字**，不是平台的元素。
我的容器选择（取包含 2–8 个选项的最深祖先）把那些注入的 UI 也吃进来了。

**危害**：题干被污染 → 本可以和已有题目合并的重复项变成新行（实测 #168 本该并入 #15），
题库虚增、查询匹配变差。

**修法**：加服务端清洗 `clean_stem()`（剥页面框架词、题型分节、题号、分值）+ `clean_miss()`
（额外剥进度条/倒计时、切掉行内选项），接在导入和待补记录两条链路上。
迁移后 20 条里 **9 条并入已有题目，且 9 组答案全部一致**——这是脚本采集与手工录入之间的交叉验证。

**踩到的坑（静默丢内容）**：进度条正则一开始写成 `\d+\s*/\s*\d+`，
把题面里的分数也吃了——「班级前1/3」变成「班级前」、「总学时的1/3」变成「总学时的」。
这份考试里 1/2、1/3 到处都是。改成斜杠**两侧必须有空格**（进度条是 `8 / 100`，分数是 `1/3`）。

**待办**：客户端（`adoptQuestions` 的题干提取）还没修——那才是根因，
服务端清洗只是兜底。修它需要看真实页面的 HTML。

## 2026-09-23 待补清单会自动划掉已补上的

考试时查不到的题会自动进待补清单；**入库时会把对应条目划掉**（模糊匹配，因为存的可能是脏文本）。
实测：32 条待补里 11 条在采集入库后被自动划掉，剩 21 条才是真空缺。

**2026-09-23 后来整个移除了**：采集入库本来就是遍历整份卷子，待补常态就是 0，
留着只是多一层要维护的机制。上面这一段作为当时的观察保留。
不划掉的话清单会越攒越假。
