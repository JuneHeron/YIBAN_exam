# 开发须知（改这个项目前先看）

这份文件记的是**做这个项目时踩过的坑**，不是功能说明。功能看 `README.md`，
油猴脚本的原理和页面结构看 `油猴/README.md`，平台相关的观察看 `NOTES.md`。

## 一、最重要的一条：不要对着 live 题库做破坏性测试

**2026-09-23 真实事故**：我为了测 `/clear` 接口，用 `source: "script"` 清库——
而当时**所有题都来自 script**，于是 231 题被删空；而且我随后那条
`rm bank-*.bak.db` 把服务端刚生成的备份也删了。

救回来的办法（记下来，万一再来一次）：

1. **立刻冻结副本** `cp bank.db bank_rescue.db`——DELETE 不会抹掉数据，被删的行还在
   文件的**空闲页**里，但后续写入会覆盖它们，所以要先冻结
2. `sqlite3 bank_rescue.db ".recover" > _recover.sql` ——`.recover` 会把空闲页里的行
   扫进 `lost_and_found` 表（列布局：`rootpgno, pgno, nfield, rowid` + 该表的 N 个字段）
3. 按 `last_seen` 取每个 `stem_key` 的最新一版，写回新库
4. **自校验**（能证明列映射没错）：`stem_norm == norm(stem_raw)`、
   `stem_key == 算出来的键`、答案字母不越界——三条都必须 0 条不符

**现在的护栏**：`clear` 按来源清时，如果会清掉九成以上，直接拒绝并提示用 `--all`。
这条护栏就是那次事故换来的。

**日常规矩**：
- 测破坏性接口（`/clear`、`/learn`）用**临时端口 + 临时目录**，别指向 `bank.db`
- 任何测试写进 live 库的数据，测完必须删掉并复查条数
- 改动前 `cp bank.db bank-手动备份.db`（便宜）

## 二、这个环境（Windows + Git Bash）的陷阱

| 陷阱 | 现象 | 正确做法 |
| --- | --- | --- |
| `pkill -f "bank.py serve"` **无效** | 你以为杀掉了，实际累积一堆进程在后台跑 | `netstat -ano \| grep 端口` 拿 **Windows PID**，再 `taskkill //F //PID <pid>` |
| `cd X && python ... &` | **`&` 把整条链（含 `cd`）放进后台**，前台还在原目录 → 在错误的目录建了空 db（我因此犯过两次） | 只把服务命令本身放后台，或先 `cd` 再单独一条 `&` |
| bat 里写 `chcp 65001` | **会吃掉 stdin**，导致粘贴的题读不进来 | 不要在 bat 里 `chcp`；`ask.py` 自己按环境选编码 |
| bat 内容里有中文 | cmd 按本地代码页（cp936）读批处理，UTF-8 存的中文会解析崩 | **bat 内容保持纯 ASCII**，中文提示交给 Python 打印（Python 输出中文没问题） |
| `sqlite3.connect("bank.db")` | 相对路径按**当前目录**解析，不是脚本目录 | 用 `DB_PATH`（基于 `__file__`），或先 `cd` 到项目目录 |

## 三、改代码时的转义陷阱（踩了 7 次）

通过 `<<'PY'` heredoc 往文件里写代码时，`\n` 和 `\\n` 会在多层传递中被还原成**真换行**，
于是字符串断成两行、语法直接报错：

```python
return lines.join('        # ← 本该是 '\n'，变成了真换行
')
```

**避免办法**：
- 写 JS/含转义的代码用 **Write / Edit 工具**（不经过 shell）
- 在 heredoc 里需要换行符时用 `chr(10)`，需要反斜杠时用 `chr(92)`
- Python 文件会自己报 SyntaxError（能立刻发现）；**JS 不会**——所以给 JS 加了检查器（见下）

## 四、改完怎么验证

```bash
python -c "import ast;ast.parse(open('bank.py',encoding='utf-8').read())"   # Python 语法
py 油猴/check_syntax.py 油猴/yiban-bank.user.js                              # JS 真·语法校验
python 油猴/check_balance.py 油猴/yiban-bank.user.js                          # JS：字符串完整性 + 括号配平
cd 油猴 && python check_algorithm.py _test_page.html                          # 识别算法（试卷页样本）
cd 油猴 && python check_algorithm.py _test_page_multi.html                    # 识别算法（答题页多选样本）
```

- **JS 现在能做真正的语法校验了**：`check_syntax.py` 用 esprima 把整个文件解析一遍。
  这条以前以为做不到（当时的结论是「代理不通，装不了 esprima」），**其实是拿错了 Python**：
  用 Windows 那个 `py`（Python 3.11，自带 pip）就能装 —— `py -m pip install esprima`。
  msys64 的 `python` 没有 pip，别拿它跑这个。教训是「装不了」要先确认试的是哪个解释器。
  注意 esprima 4.0.1 只支持到 ES2017：`?.` / `??` / `||=` 这类会**误报语法错**，
  真要用新语法得升级 esprima 或换 node。
- `check_balance.py` 仍留着：它查**跨行字符串**和括号配平，和语法校验互补——
  这个文件被 heredoc 的转义吃掉过多次，那种伤只有它能查出来。
- `check_algorithm.py` 是**把 JS 里的识别规则用 Python 等价重写**，喂真实 DOM 副本的
  `_test_page*.html`。改识别逻辑时，两份样本都要过。
  **注意**：0 道题必须判失败——早先版本在空结果下也报「通过」，是假通过。
- 改服务端后，起个临时端口把 `/ask`、`/ask-batch`、`/learn` 各测一遍；
  尤其要测「**查不到**」这条路径（曾经因为留着已删函数的调用而崩）。

## 五、文件布局

```
bank.py                    工具：查询 + 桥接服务 + 维护命令
start.bat                  给新手的一键启动（ASCII，找 python/py）
README.md                  给使用者的说明
NOTES.md                   平台相关的观察记录（含历史决策）
开发须知.md                 本文件
油猴/yiban-bank.user.js     油猴脚本
油猴/README.md              脚本原理 + 页面结构 + 风险
油猴/check_*.py             三项检查（语法 / 括号配平 / 识别算法）
油猴/_test_page*.html       真实 DOM 样本（识别算法的验证输入）
发布/                       给别人用的打包副本（不含 bank.db）
_恢复备份/                  误删事故的恢复文件，可删
```

`bank.py` 里 `USERJS_PATH = Path(__file__).with_name("油猴") / "yiban-bank.user.js"`——
所以 `油猴/` 这个**目录名和层级不能改**，否则安装页会报「脚本文件不在」。

## 六、几条设计决定（改之前先理解为什么）

- **答案存字母 + 选项原文两份**：平台每次进入会打乱选项，字母只对当时那次的顺序有效，
  选项原文才稳定。所以查询时必须**连选项一起提交**，服务端按选项原文反查本次的字母。
- **题目身份默认只取题干**；但题干是**纯套话**（「下列说法错误的是」）时要把**选项集合**
  也算进去——否则同一句套话下的多道题会撞成一道、答案互相覆盖。
- **冲突比较用选项原文，不用字母**：字母不同但原文一致 = 同一答案。
- **平台里的选项匹配要放宽三级**（严格相等 → 去首尾空白 → 按 norm 比对）：
  题库里的选项来自采集时那次渲染，答题时是另一次渲染，可能只差标点或空白。
  匹配太严会导致算不出字母、脚本只能随机猜（症状：「错了很多题但库里都有」）。
- **答题走两遍**（先只读收集 → 一次批量问 → 再逐页点选）：每道题一次网络请求的话，
  每次 `await` 都会让浏览器有机会重绘，于是看得见翻页。**这是唯一的路径**——
  原来面板上还有个「快速模式」勾选框，不勾就走逐题问的老路（慢、像人在点）；
  2026-09-24 去掉了，老路只剩「过程看得见」一个好处，不值得多维护一份遍历逻辑。
  副作用：**没有「看起来像人在点」这个伪装了**，要的话得自己加回等待。
- **遍历翻页的探测间隔要小**（20ms）：原来 100ms 一探，只要 React 更新晚一帧就白等 100ms，
  100 道题累积十几秒。
- **没有手工录入**：题目只由油猴脚本在交卷后的试卷页采集。
